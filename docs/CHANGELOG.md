## Changelog

### 2026-07-17 (чаты без автоочистки)
- История ЛС больше не обрезается (раньше: 100 сообщений на диалог, 40 диалогов) — удаление только игроком.

### 2026-07-17 (cleanup Ah)
- Удалён неиспользуемый конфиг `Ah` / `feeRate` (продажа лота через меню не делается).

### 2026-07-17 (репозиторий)
- Подготовка приватного GitHub-репозитория `LavaMenu`: из публикации исключены agent-only docs (`AI_CONTEXT`, `SESSION`, `TASKS`); README без внутренних путей.

### 2026-07-17 (канон LavaMenu)
- Каноническая копия проекта на диске синхронизирована с JAR; `release/lavamenu-0.1.0.jar` без изменений.
- `source/` приведён к состоянию JAR (Чаты, safe rename, лица игроков, radial shop/chats и т.д.).
- Корневой `LICENSE`; README и docs (PROJECT/TASKS/COMPATIBILITY) синхронизированы с текущим модом.

### 2026-07-16 (головы игроков)
- Головы в Чатах / Друзьях / шапке диалога: онлайн из списка Tab/P (`PlayerFaceExtractor`); кэш `config/lavamenu/faces/`; иначе инициалы + точка статуса.

### 2026-07-16 (radial Чаты)
- В настройках быстрого меню (G) можно выбрать действие **Чаты** — открывает вкладку Чаты.

### 2026-07-16 (fix scissor / пустой диалог)
- `MenuPanel.withScissor`: MC 26 `enableScissor(x1,y1,x2,y2)` — из‑за `(x,y,w,h)` сообщения в диалоге вырезались (пустой чат).

### 2026-07-16 (Чаты list polish)
- Список диалогов: время `HH:mm`, превью через `plainSubstrByWidth` + «…», статус у ника; клип длинных подписей вкладок.

### 2026-07-16 (Чаты list UX)
- Вкладка «Чаты»: список как у «Друзей» — без кнопок-карточек; ник + превью + время + непрочитанные; клик по строке; иконка отправки вместо «Написать».

### 2026-07-16 (fix чаты wrap)
- Диалог: длинные сообщения переносятся по ширине панели вместо обрезания «…».

### 2026-07-16 (fix чаты PM + layout)
- Входящие ЛС: структурный парсер `[PM]…`, overlay тоже; не rebuild экрана на каждое сообщение.
- Диалог: короткая переписка прижата к низу (над полем ввода), не к верху.

### 2026-07-16 (Чаты)
- Вкладка **Чаты**: список диалогов (новее сверху), онлайн/офлайн, непрочитанные, дата/время, удаление; диалог с `/msg`; парсер `[PM]…`; история в `config/lavamenu-chats.json`.

### 2026-07-16 (radial hint)
- Колесо: убрана подсказка «Клик или повтор G — закрыть».

### 2026-07-16 (fix quick homes dim)
- Экран выбора точки из колеса: иконки измерений (Край и т.д.) как в основном меню.

### 2026-07-16 (fix dim headers widgets)
- Заголовки «Край» / «Верхний мир» в списке точек через виджеты — иконка измерения снова должна отображаться.

### 2026-07-16 (radial shop)
- Быстрое меню: действие «Магазин» → `/warp shop` (выбирается в настройках слотов).

### 2026-07-16 (fix dim headers)
- Заголовки измерений в списке точек снова видны (иконка + «Край» / «Верхний мир»).

### 2026-07-16 (fix settings layout)
- Вкладка «Настройки»: разведены строки подсказки клавиш и заголовка слотов G.

### 2026-07-16 (fix /homes ещё раз)
- Парсер: заголовок без обязательного `LAVA >`; события CHAT+GAME; debug-строки в лог после обновления списка.

### 2026-07-16 (fix /homes формат)
- Парсер: префикс `[LAVA] >` (со скобками) и буллет `▪`/`•`; имена в одной строке с миром и с переносом чата.

### 2026-07-16 (fix /homes)
- Восстановлен разбор имён на отдельной строке после заголовка мира (регресс после P0-2).

### 2026-07-16 (radial PvP)
- Колесо: слот PvP показывает «PvP выкл» / «PvP вкл» по текущему статусу из Tab (иконка щит/меч).

### 2026-07-16 (docs)
- Подтверждена официальная справка LavaWin: нет атомарного rename; координаты точек в hover tooltip `/homes` (заметка для будущих фич).

### 2026-07-16 (P0)
- **P0-1:** безопасное переименование точек — без `/home`+таймеров; подтверждение в UI; `/delhome` только после появления нового имени в `/homes`.
- **P0-2:** парсер `/homes` только по префиксу `LAVA >`, сессия с концом по `(N/M)` или idle-таймауту; warn в лог на мусор/таймаут.

### 2026-07-16
- **PvP:** статус из Tab (`PvP режим включен/отключен`), синхронизация переключателя; действие «PvP вкл/выкл» в колесе.
- Fix: поля ввода на вкладке «Друзья» больше не сбрасываются при обновлении онлайна.
- Fix: быстрое меню в режиме удержания больше не мигает — hold читает физическую клавишу из Minecraft Controls.
- **Radial:** хоткей `G` перенесён полностью в Minecraft → Управление; внутри LavaMenu больше нет собственного переназначения.
- **Radial:** убран grace-период удержания — действия по отпусканию клавиши (в т.ч. `/ah`) должны срабатывать стабильнее.
- **Настройки:** возвращён заметный переключатель режима удержания и подсказка, что клавиши меняются в Minecraft.
- **Избранные точки:** `1` точка — мгновенный `/home`, `2+` — отдельный список избранных.
- **Друзья:** зелёный/серый индикатор онлайна, сортировка (сначала в сети), счётчик «N / M онлайн».

### 2026-07-14 (v0.4.4)
- **Точки:** пиксельный скролл — подписи миров (Край, Ад) снова на месте, не перекрываются строками.
- **Точки:** «Сохранить» — обычный текст справа, «Перезапись» — кнопка слева.
- **Настройки G:** убрана пустая «ручка», иконки как в быстром меню, подсказка «слева вкл/выкл, строка — действие».
- **Быстрое меню (G):** пилюли по центру дуги, аватар Mzrb не обрезается, рисуется поверх кнопок.

### 2026-07-14 (v0.4.3)
- **Точки:** поле имени на отдельной строке; «Перезапись» и «Сохранить» рядом внизу — без наложения.
- **Точки:** заголовки миров (Верхний мир / Край / Ад) синхронизированы со списком, иконки не прячутся под капом.
- **Настройки:** убраны «Сбросить клавишу G», переключатель быстрого меню и «Сбросить сектора».
- **Настройки:** остались привязки R/G, режим удержания и список слотов — выше, видно все 8 без прокрутки.
- Быстрое меню (G) всегда включено.

### 2026-07-14 (v0.4.2)
- Полный аудит вёрстки: фиксированные зоны формы и списка, без наложений при скролле.
- Точки: «Сохранить» выровнена, список не залезает на поле ввода.
- Команды: подписи секций не перекрывают кнопки; PvP-переключатель на своей строке.
- Друзья: кнопка «+» с иконкой вместо голубого квадрата.
- Настройки: сектора radial выше, видно ~6–7 без прокрутки.

### 2026-07-14 (v0.4.1)
- Ещё компактнее: панель 300×268, строки 16px, иконки 12px, вкладки 16px.
- «Сесть/Лечь» — обычные строки (не высокие блоки), друзья в одну строку.
- Убраны лишние рамки у кнопок, меньше radial-пилюли.

### 2026-07-14 (v0.4.0)
- **Fix иконок:** blit вызывался с перепутанными UV — иконки не рисовались (белые/красные квадраты).
- **Компактный UI:** шапка 20px, вкладки 18px, строки 20px — в список точек влезает ~12+ штук.
- Убраны лишние отступы и дублирующие заголовки секций.

### 2026-07-14 (v0.3.4)
- Прокрутка: scissor-обрезка списков, виджеты только внутри зоны, колёсико работает только над списком.

### 2026-07-14 (v0.3.3)
- Иконки уменьшены: 12×12 в кнопках/строках (было 16×16), кнопки 22px вместо 26px.
- Вкладка «Настройки»: слоты radial сдвинуты ниже — больше не наезжают на переключатели.

### 2026-07-14 (v0.3.2)
- Иконки v2 из Cloud Code: vanilla-MC bevel, улучшенные силуэты (атлас `icons.png`).
- Обновлён `tools/generate_icons.py` + превью `icons_preview_v2.png`.

### 2026-07-14 (v0.3.1)
- Иконки меню: атлас PNG `textures/gui/icons.png` (23 иконки 16×16) вместо векторных «квадратиков».
- `GuiIcons` рисует через `blit` из атласа; перегенерация: `python3 tools/generate_icons.py`.

### 2026-07-14 (v0.1.8)
- Анимации sit/lay: sneak только серверным пакетом, без `KeyMapping` Shift — не триггерит Inventory Profiles (переодевание в инвентаре).

### 2026-07-14 (v0.1.7)
- Вкладка «Быстрое»: компактнее, выше, прокрутка слотов колёсиком, подсказка про свою картинку.
- Radial (G): меню смещено вверх — не перекрывает сердца/голод.
- Radial: чёрный квадрат в центре заменён на иконку Mzrb (вшита в мод); опционально override: `config/lavamenu/radial_center.png`.

### 2026-07-14 (v0.1.6)
- Radial: убран pickSector (из-за него аукцион открывал точки/меню) — только клик по кнопке.
- Radial: вкл/выкл каждого сектора (✓/○) в R → Быстрое; по умолчанию 5 секторов.
- ConfirmScreen / RenameHomeScreen: текст виден (рисуется до render).
- Анимации: короткий тап левого Shift перед сменой sit↔lay.

### 2026-07-13 (v0.1.5)
- Homes list: widget-кнопки вместо невидимого текста после super.render (MC 26).
- Авто-обновление списка точек при ответе сервера в чат.
- Radial → «Меню точек» / «Меню»: больше не закрывается сразу после открытия.
- Шире панель, вкладка «Быстрое» не обрезается.

### 2026-07-13 (v0.1.4)
- Homes parser: формат LavaWin «Верхний мир: дом1 дом2», префиксы LAVA > и •.
- Radial: видимые кнопки-сектора, фон через extractBackground, toggle через физическую клавишу G.
- Animations: tracked-state вместо pose; длинный Shift-пульс перед сменой sit/lay.

### 2026-07-13 (v0.1.3)
- Переключение анимаций: при смене sit↔lay автоматически имитируется Shift (отмена текущей позы), затем новая команда.
- Определение активной анимации по позе игрока (SITTING/SLEEPING) и последней отправленной команде.

### 2026-07-13 (v0.1.2)
- Fixed silent button failures: UI commands bypass global cooldown.
- Commands from menu close GUI and send on next tick (sit/lay/ah/tp).
- Action bar feedback for sent commands and validation errors.
- Homes counter is rendered text (not a clickable widget).
- Widget layout only updated in init/reposition (not every render frame).

### 2026-07-13 (v0.1.1)
- Fixed radial hold mode flashing (GLFW physical key check + grace ticks).
- Fixed homes tab layout: full-width refresh button, title/tabs no longer overlap.
- Fixed animation commands: close menu before sending `/sit` / `/lay`.
- Radial actions close overlay before sending server commands.

### 2026-07-13
- Added `docs/` and initialized documentation for new `lavamenu/` subproject.
- Added initial `lavamenu/` Fabric mod skeleton (main menu on `R`, AH UI with 15% fee preview, config stub).
- Added radial menu mode setting (hold vs toggle).
- Full v0.1 feature set: homes parser, AH sell UI, animations, in-menu keybinds, configurable radial wheel.

