# Положите сюда JAR других модов

Эта папка — для модов, которые ставятся **рядом** с LavaMenu в одной сборке.

При установке копируйте всё из `release/` и `mods/` в `.minecraft/mods/`.

Список и результаты проверки совместимости: `docs/COMPATIBILITY.md`
